// This program is a simple test that external declarations work properly

program Test {
  external int i;
  external procedure Test;

  void Test() {
    read i;
    int sum;
    sum = 0;
    while (i > 0) {
      sum = sum + i; i = i - 1;
    }
    write sum;
  }
}
