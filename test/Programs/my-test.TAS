program MyTest {

    const int number := 2;
    int x;
    string str := "a string";

    void testFive() {
        const int c := 5;
        if (c >= number) write c; else write number;
        write c;
        write number;
    }

    void is1() {
        read x;
        if (number = 1) write 1; else write 0;
    }

    void Main() {
        testFive();
    }
}
